# ArtTag-Discord.js-Bot
Discord.js bot for storing art image links from various websites.

### Getting Started

1. Clone repo, run `npm i`
2. Copy `config-sample.json` and rename to `config.json`
3. Fill in `config.json` values
4. Run `npm start`